# Firebase Configuration
FIREBASE_SERVICE_ACCOUNT_PATH=./serviceAccountKey.json
# Or use FIREBASE_SERVICE_ACCOUNT_KEY with the JSON content directly

# Server Configuration
PORT=3000
JWT_SECRET=your_jwt_secret_here_change_this_for_production

# These are only needed if falling back to local storage
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=tcc_evoting